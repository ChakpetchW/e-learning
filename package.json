{
  "name": "elearning-monorepo",
  "private": true,
  "scripts": {
    "build": "npm run build-api && npm run build-webapp",
    "build-api": "cd elearning-api && npm install && npx prisma generate",
    "build-webapp": "cd elearning-webapp && npm install && npm run build && mv dist ../dist"
  }
}
